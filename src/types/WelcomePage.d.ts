export default interface WelcomePage {
  background: string
  progress: {
    background: string
    foreground: string
  }
  tileBackground: string
  tileHoverBackground: string
  tileShadow: string
}
