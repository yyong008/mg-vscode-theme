export default interface Breadcrumb {
  foreground: string
  background: string
  focusForeground: string
  activeSelectionForeground: string
}

export interface BreadcrumbPicker {
  background: string
}
