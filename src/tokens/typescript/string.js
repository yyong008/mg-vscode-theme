import colors from '../../designs/index.js'

const tsString = {
  name: 'ts::string)',
  scope: ['string.quoted.single.ts', 'string.quoted.double.ts'],
  settings: {
    foreground: colors.grayColors600
  }
}

export default [tsString]
