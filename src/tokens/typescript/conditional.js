import colors from '../../designs/index.js'

const tsDitional = {
  name: 'ts::conditional',
  scope: ['keyword.control.conditional.ts'],
  settings: {
    foreground: colors.magentaColors600
  }
}

export default [tsDitional]
