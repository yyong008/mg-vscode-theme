import colors from '../../designs/index.js'

const regexp = {
  name: 'lua::regexp',
  scope: ['string.regexp.lua'],
  settings: {
    foreground: colors.purpleColors400
  }
}

export default [regexp]
